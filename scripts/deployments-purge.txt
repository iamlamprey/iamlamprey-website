$repo = 'repos/iamlamprey/iamlamprey-website'; $head = (git rev-parse HEAD).Trim(); $deps = @(gh api "$repo/deployments?per_page=100" | ConvertFrom-Json | Where-Object { $_.sha -ne $head }) | Sort-Object id; $deps | ForEach-Object { gh api -X POST "$repo/deployments/$($_.id)/statuses" -f state=inactive 2>$null | Out-Null; gh api -X DELETE "$repo/deployments/$($_.id)" 2>$null | Out-Null }; $deps | ForEach-Object { gh api "$repo/deployments/$($_.id)" --silent 2>$null; if ($LASTEXITCODE -ne 0) { Write-Host "deleted $($_.id)" } else { Write-Host "STILL THERE $($_.id) $($_.environment) $($_.sha)" } }; @(gh api "$repo/deployments?per_page=100" | ConvertFrom-Json) | Format-Table id, environment, sha, created_at -AutoSize
