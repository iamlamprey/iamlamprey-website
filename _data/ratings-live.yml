# Generated by .github/workflows/ratings.yml — do not edit by hand.
#
# This site's own ratings, mirrored from the Worker's /api/ratings. A product
# uses this file once it has 10 of them; below that
# _includes/product-rating.html falls back to the pooled marketplace figures in
# ratings.yml, or shows no badge at all. Every rating counts, including the low
# ones, and the display rule is there rather than here.
#
# generated_at dates the figures rather than the hourly run, so it arrives with
# the first one: an empty product block is a site with no ratings yet.
product:
